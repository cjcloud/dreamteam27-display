# Saved theme snapshot — "prussian/aquamarine" (pre-modern-palette exploration)

Backup of the display app's theme files as of the current version.
To restore: copy globals.css and tailwind.config.js back to their locations
(app/globals.css and tailwind.config.js).

Note: the prussian and aquamarine [data-theme] blocks also remain in the live
globals.css, so reverting is usually just a matter of switching the default
data-theme back to "prussian" in app/layout.tsx.
